import React from "react";

const Header: React.FC = () => {
    return (
        <div className="title-bar">
            <h1 className="title">Anoter To-Do List</h1>
        </div>
    );
};

export default Header;
